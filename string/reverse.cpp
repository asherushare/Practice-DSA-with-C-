#include <iostream>
#include <string>
using namespace std;

void reversStr(string &str) {
  // int n = str.length();

  // for(int i = 0; i < n/2; i++) {
  //   swap(str[i], str[n - i - 1]);
  // }
}

int main() {

  string str = "Patra";
  // reversStr(str);
  // cout << str << endl;

  // reverse(str.begin(), str.end());
  // cout << str << endl;

  return 0;
}